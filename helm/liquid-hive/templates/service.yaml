apiVersion: v1
kind: Service
metadata:
  name: {{ include "liquid-hive.fullname" . }}
  labels:
    {{- include "liquid-hive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liquid-hive.selectorLabels" . | nindent 4 }}